# SJ Hub CA 정산 개선판 (settlement-v2) — 작업 인수인계 문서

> 다른 AI 에이전트/개발자가 이어서 작업할 수 있도록 전체 맥락을 정리한 문서.
> 최종 업데이트: 2026-07-16 · 현재 버전: **v3.3**

---

## 1. 서비스 배경

**SJ Hub**는 3단 유통·판매·정산 플랫폼이다.

- **CA (운영사 어드민)**: SJ Hub 본사. 전체 조직·대리점·정산을 관리하는 최상위 어드민. 조직 관리 + 일정산 집행이 핵심. 은행 미연동 — 시스템 밖에서 송금 후 정산 상태를 수동 변경.
- **SA (영업조직 어드민)**: 지사(1단계) → 총판(2단계) → 영업(3단계) 위계의 영업조직이 사용. "받을 정산금 / 하위에 내려줄 정산금 확인"이 목적. 조회 중심.
- **BA (대리점 어드민, 제품명 link it)**: 대리점(=브랜드=파트너사=가맹점, 모두 동일 주체)이 상품·상점·주문·배송을 실제 운영.
- 대리점은 지사/총판/영업 어느 단계에서든, 또는 CA와 직접 연결될 수 있음 (신청 출처 5분류: CA 직접 등록 / 지사 등록 / 총판 등록 / 영업 등록 / 대리점 직접 신청).
- 셀러: 대리점이 BA에서 등록하는 하위 판매 계정. 별도 셀러 어드민 존재. 현 개편에서 비중 낮음.

참고 문서:
- 서비스 가이드: `~/Documents/Claude/Projects/SJ HUB/docs/sjhub-admin-guide/sjhub-ca-sa-ba-service-guide.html`
- 데이터 구조 분석(이슈 S-01~S-17 도출): `~/Documents/Claude/Projects/SJ HUB/docs/sjhub-admin-guide/ca-settlement-data-structure.html`

## 2. 파일 구조

```
~/Desktop/linkit-main/SJhub-CA/          ← 원본 CA 어드민 프로토타입 (수정 금지, 비교용)
  ca-013-settlement.html 등              원본 정산 화면 (settlement-pages.js 구동)
  shared.css / shared.js                 공용 스타일·유틸 (v2가 재사용)
  settlement-v2/                         ← 개선판 (이 폴더가 작업 대상)
    index.html                           개선판 안내 + 버전별 반영 내역 표 (v2/v2.1/v2.2/v2.3)
    settlement-v2.css                    v2 전용 보조 스타일
    settlement-v2.js                     ★ 데이터 모델 + 전체 로직 (단일 파일)
    ca-013-settlement.html               지사 정산   (body data-v2-mode="list" data-v2-type="branch")
    ca-019-settlement-distributor.html   총판 정산   (type="distributor")
    ca-020-settlement-sales.html         영업 정산   (type="sales")
    ca-021-settlement-brand.html         대리점 정산 (type="brand")
    ca-014-settlement-detail.html        정산 상세   (mode="detail", URL ?type&id&base)
    ca-022-settlement-reserve.html       유보금·예치금 관리 (mode="reserve", ?tab=deposit)
    HANDOFF.md                           ← 이 문서
```

구동 방식: 각 HTML은 얇은 셸. `../shared.js`(유틸·모바일 헤더) 로드 후 `settlement-v2.js`가
`body`의 `data-v2-mode` / `data-v2-type`으로 분기해 사이드바·테이블 헤더·데이터를 전부 렌더링.
shared.js가 만든 사이드바는 v2용으로 덮어씀(`buildSidebar()`).

## 3. 버전 이력 (모두 settlement-v2.js에 구현됨)

### v2 — 데이터 구조 이슈 17건 개선 (S-01~S-17)
원본 분석에서 발견한 이슈. 핵심: 단일 원천 데이터 파생(KPI 하드코딩 제거), (대상×회차) 정산 로우,
차등(포함형) 수수료 분배, 취소 반영 공식 통일, parentId FK + 위계 검증(지사→총판→영업),
상태 전이표(필요→대기→중→완료, 완료 취소는 사유 필수 + 이력), 용어 단일화.

### v2.1 — 유보금·예치금 개편 (운영 정책)
운영 어드민에서 상점관리 팝업에 있던 유보금/예치금을 **정산관리의 탭 화면(ca-022)**으로 이동.
- 목적 동일: 정산 완료 후 결제 취소 발생 대비금.
- **유보금**: 결제마다 비율 자동 적립 — 수동 등록 버튼 없음. '사용' 버튼만(사유 필수).
  한도 도달 시 적립 중단 + **인출 알림**(배너/뱃지) + 인출 처리 액션.
  **90일+ 무사고 보유 시 '사용등록' 기록** 액션(관리자가 시점·메모 기록).
- **예치금**: 미리 받아두는 대비금. 등록/차감 수동, 사유 필수, 잔액 검증.
- 행 삭제 금지 → **역분개(정정)** 방식. 자동 적립 건은 수정 불가.

### v2.2 — 정산 방향·주기·취소 정책 (담당자 피드백 2026-07-15)
- **결제금액(원금) 기준.** 대리점 수수료 = 운영사가 떼는 몫.
  → 대리점 지급 = 결제원금 − 수수료 − 유보금.
- **유보금 = 결제 원금 × 유보율**, 한도까지 계속 차감.
- **조직 수수료는 수수료 풀 안에서 지급**: 남은 수수료로 조직 차등 지급, 잔여 = CA 마진.
  검증: 체인 최상위 조직율 ≤ 대리점 수수료율. (분배 보기 모달 = 결제원금 100% 분해)
- **전역 회차(일/주/월) 폐지 → 정산 기준일 + 대상별 주기(D+N일)**.
  오늘 정산 매출 = 각 대상의 주기만큼 이전에 결제된 매출 (`paidDate == 기준일 − cycleDays`).
- **취소**: 기준일에 확정된 취소(이전 결제분)를 매출에서 차감 — 대리점·조직 동일.
  차감 > 매출이면 음수 표시 + "익일 이월" 경고 (실제 이월 로직은 미구현 — 정책 미정).
- **당일 주문 + 당일 취소는 모든 계산에서 제외** (`isSameDayCancel`).

### v2.3 — 4개 정산 페이지 컬럼 개편 (클라이언트 요청, 원문 중국어)
지사·총판·영업·대리점 페이지 공통:
1. '정산 금액(结算金额)' 컬럼명 → **'수수료(手续费)'**
2. **'정산 예정 금액(应结算金额)'** 신설 = 매출 − 수수료
3. **'정산 상태(结算状态)' 컬럼 삭제** — 보이지 않는 상태 기준 검색을 피하기 위해 상태 필터도 삭제. 단, 상태 워크플로우(변경/지급완료)는 유지,
   개별 상태는 상세 화면의 "기준일 상태" 배너에서 확인.

### v2.4 — 테이블 데이터·공식 확정 (클라이언트 레퍼런스 테이블)
클라이언트가 지사(分公司) 페이지 레퍼런스 테이블(10행, 원 단위 수치)을 제공 → 데이터 레이어 재구성.
- 4페이지 공통 컬럼: 명칭 | (소속/출처) | 수수료율 | 총 판매금액 | 정산 건수 | 수수료 | 정산 예정 금액 | 상세
- **공식 확정: 수수료 = round(총판매금액 × 수수료율) · 정산 예정 금액 = 총판매금액 − 수수료**
- 각 레벨 수수료 **독립 계산** (v2의 차등 분배 모델 폐기 — 클라이언트 수치로 확정됨)
- 데이터: 지사 = 레퍼런스 10행 그대로 (100,000/17건/7% …). 총판·영업·대리점 = 운영 스크린샷
  시리즈 (sales = 80,000 + i×43,120 · count = 8 + i×3 · fee 사이클: 총판 6,7,8,5… 영업 5,6,7,4… 대리점 7,8,9,6…)
- 상세 내역은 목록 집계와 정확히 일치하게 분해 생성 (`makeDetailRows`: 판매금액 합 = 총판매금액,
  수수료 합 = 수수료, 행 수 = 정산 건수, 마지막 행이 반올림 흡수)
- v2.2의 주기(D+N)·취소 차감 컬럼은 목록에서 제거 (레퍼런스 레이아웃 준수). 기준일 패널은
  상태 기록 단위로만 유지. 유보금 차감은 목록 미표시 — 유보금·예치금 관리에서 처리, ⚠/⏱ 뱃지로 연결
- v2.3의 열린 질문(조직 페이지 정산 예정 금액 의미) → **매출 − 수수료로 확정, 해소**

### v2.5 — KPI 6종 카드 + 상태 관리를 상세 페이지로 이동
- **KPI 6종 카드** (4개 목록 + 상세 공통): 총 정산 수 / 정산 완료 건 / 정산 필요 건 /
  정산 총액 / 정산 완료 금액 / 정산 필요 금액 — 전부 **거래(건) 단위** 집계라 목록·상세 항상 일치.
  금액 기준 = 지불해야 할 정산 금액(settle). 필요 = 완료가 아닌 모든 상태.
- **목록 테이블 = 스펙 컬럼만**: 명칭 - (소속/출처) - 수수료율 - 총 판매금액 - 정산 건수 -
  수수료 - **지불해야 할 정산 금액** - 상세내역. 체크박스·상태 컬럼·상태 필터·일괄 버튼 제거.
- **정산 상태·상태 변경·상태별 필터는 상세 페이지(ca-014)로 이동**: 거래 행마다 상태 뱃지,
  체크박스 + 전체 선택 + 선택항목 상태 변경(전이표 강제) + 지급완료(정산중만) + 상태 필터.
- 상태 저장: `txStatusStore[ownerId] = 상태배열` (상세 행 인덱스 정렬, `getTxStatuses`).
  시드: `_01` 대상은 전건 '정산 완료'(지사_01 = 17건/93,000원 완료 데모), 나머지는 5종 순환.
- `statusStore`(대상×기준일 단위) 제거 — 상태는 거래 단위로 일원화.
- **상태 3단계로 단순화**: 정산 필요 → 정산중 → 정산 완료. 색상 구분 뱃지(`.v2-st--need` 빨강 /
  `.v2-st--ing` 주황 / `.v2-st--done` 초록, settlement-v2.css). 전이: 필요→중, 중→완료|필요(되돌리기),
  완료→중(완료 취소, 사유 필수). '정산대기'·'정산 보류' 상태 폐기.

### v2.5 — KPI 6카드 및 중국어 번역 보정
지사·총판·영업·대리점 목록 상단 KPI를 **총 정산 수 / 정산 완료 건 / 정산 필요 건 / 정산 총액 / 정산 완료 금액 / 정산 필요 금액** 6개 카드로 통일.
금액 KPI는 클라이언트 피드백 기준의 **应结算金额 = 总销售金额 − 手续费**(`payable`) 합계로 계산한다.
중국어 사전 보정: `수수료 → 手续费`, `정산 예정 금액 → 应结算金额`, `정산 총액 → 应结算总额`, `총 정산 수 → 总结算件数`.

### v2.6 — 의미 없는 상태 필터 제거
목록 테이블에서 `结算状态` 컬럼을 삭제한 뒤, 보이지 않는 상태값으로 검색하는 `정산상태 전체` 필터가 혼동을 만들어 제거.
상태 데이터는 KPI(정산 완료/필요)와 상태 변경·지급완료 워크플로우를 위해 내부적으로만 유지한다.

### v2.7 — 목록 4페이지 정적 하드코딩 전환 (사용자 요청 2026-07-16)
지사·총판·영업·대리점 목록(ca-013/019/020/021)을 settlement-v2.js 렌더링 대신 **정적 HTML로 하드코딩**.
- 값 산출: settlement-v2.js를 그대로 Node에 로드해 `toRow`/`txAgg`/`retentionStatus` 등 내부 함수를 호출한 스냅샷
  (검증 방법 5절과 동일한 mock-DOM 스텁 패턴, `window.__v2 = {...}` 훅 주입). 그래서 이전 화면과 수치가 완전히 동일함.
- 유지: 사이드 네비게이션·모바일 헤더(shared.js), 상세(ca-014), 유보금·예치금(ca-022) — 전부 기존 script 구동 그대로.
  ca-014는 URL 쿼리(?type&id)로 대상별 콘텐츠가 갈리는 페이지라 대상 수(약 40여개)만큼 정적 파일을 만들어야 해서 제외(사용자 확인).
- 목록 4페이지에서 제거된 것: `settlement-v2.js` 스크립트 태그, `data-v2-mode`/`data-v2-type`, 정렬 버튼, 페이지네이션, pageSize 선택, 엑셀 다운로드.
  대신 남긴 것: 이름 검색(각 페이지 하단에 동일한 최소 인라인 스크립트로 처리, 페이지 새로고침 시 항상 리셋).
  기준일 패널은 입력 불가 정적 텍스트로 단순화(원래도 목록 페이지에서 지급완료 버튼은 숨겨져 있었음).
- **주의**: 이 4페이지는 이제 SALES 단일 원천에서 파생되지 않고 고정 스냅샷이다. v2.4~v2.6까지 지켜온
  "모든 화면 수치는 단일 원천에서 파생, 하드코딩 KPI 금지" 원칙과 상충하지만 사용자의 명시적 요청에 따른 의도적 예외.
  데이터를 바꾸려면 각 HTML의 KPI/테이블 마크업을 직접 수정해야 하며, settlement-v2.js의 BRANCH_DATA 등을 고쳐도
  이 4페이지에는 반영되지 않는다(ca-014/ca-022에는 계속 반영됨 — 두 소스가 분리되었으므로 향후 값 변경 시 양쪽 다 갱신 필요).

### v2.8 — 거래 상태 3단계 고른 분배 (사용자 요청 2026-07-16)
`getTxStatuses`(settlement-v2.js)에 있던 "`_01` 대상(지사_01/총판_01/영업_01/대리점_01)은 전건 정산 완료" 특례를 제거.
이제 모든 대상이 동일하게 `STATUSES[(i + idNum) % 3]` 순환 배분을 받아 필요/중/완료 3단계가 항상 고르게 섞여 보인다
(ca-014 상세 페이지 거래 목록·상태 뱃지 기준). 상세 페이지는 계속 script 구동이라 자동 반영되지만,
v2.7에서 하드코딩한 목록 4페이지 KPI(완료 건/필요 건/완료 금액/필요 금액)는 스냅샷이라 이 변경으로 값이
바뀌어 재계산 후 4개 HTML에 반영함 (총 정산 수·정산 총액은 대상 수·판매금액 기준이라 변동 없음).

### v2.9 — 목록 4페이지 컬럼 재구성: 정산주기·적용매출일 도입 (사용자 요청 2026-07-16)
목록 4페이지(ca-013/019/020/021, 상세 ca-014는 이번에도 제외)의 테이블 컬럼을 다음 8개로 전면 교체:
**명칭 - 수수료율 - 정산주기 - 적용매출일 - 실매출액 - 정산 건수 - 정산 금액 - 상세내역**.
- **제거**: 소속/신청출처 등 조직 관계 컬럼(총판의 "소속 지사", 영업의 "소속 총판·소속 지사",
  대리점의 "신청 출처·관리 조직") 전부 삭제 — 요청받은 8컬럼 스펙에 없어서 드롭. 절대금액 "수수료" 컬럼도
  삭제(수수료율만 표시, 정산 금액에 내부적으로 반영).
- **이름 변경**: 총 판매금액 → **실매출액**, 지불해야 할 정산 금액 → **정산 금액**
  (공식 불변: `정산 금액 = 실매출액 − 수수료`, `수수료 = round(실매출액 × 수수료율 / 100)` — v2.4 공식 그대로,
  변수명만 스펙에 맞춤). 각 테이블 위에 공식 안내 문구 추가(`v2-kpi-note`).
- **신규 도입 — 정산주기·적용매출일**: v2.2에서 있었다가 v2.4 레퍼런스 레이아웃 채택 시 화면에서 빠졌던
  "대상별 주기(D+N일)" 개념을 목록 컬럼으로 부활. mock: `CYCLE_DAYS = [7,3,15,1]`를 4페이지 공통으로 행
  인덱스(0~9)에 순환 배분(`[7,3,15,1,7,3,15,1,7,3]`), 적용매출일 = 기준일(2026-07-07) − 정산주기
  (UTC 기준 날짜 계산 — 로컬 타임존으로 하면 하루 밀리는 버그 있었음, `Date.UTC` 사용해 회피).
  이 cycle/date 값은 settlement-v2.js에 반영된 게 아니라 각 HTML에만 하드코딩됨(ca-014는 그대로 D+N 개념 없음).
- 테이블 `min-width`를 1240px → 1080px로 축소(컬럼 수 감소 반영). 필터 스크립트는 그대로
  (여전히 `tr.children[0]` = 이름 셀이라 영향 없음).
- **중국어판(shared.js `ZH_REPLACEMENTS`) 갱신**: `정산주기→结算周期`, `적용매출일→适用销售日`,
  `실매출액→实际销售额` 3개 신규 추가(기존 `수수료율`·`정산 건수`·`정산 금액`·`기준일` 항목 재사용).
  치환은 최長 매칭 우선(`ZH_REPLACEMENTS_SORTED`)이라 "실매출액" 같은 합성어가 부분 문자열("매출")보다
  먼저 치환되어 깨지지 않음 — 새 라벨을 추가할 때는 항상 전체 문자열 엔트리를 넣어야 함(부분 문자열만
  있으면 "실销售额액"처럼 깨짐).

### v2.10 — 정산기간 기간검색(날짜:시간~날짜:시간) 추가 (사용자 요청 2026-07-16)
목록 4페이지(ca-013/019/020/021)의 "적용매출일" 컬럼에 시간을 추가하고, 이를 기준으로 한 기간검색 UI를 도입.
- **적용매출일에 시간 추가**: 기존 날짜만 있던 값(예: `2026-06-30`)에 시각을 붙여 `2026-06-30 09:15` 형태로 표시.
  mock 시각은 `TIMES = ["09:15","14:30","11:00","20:45"]`를 정산주기(`CYCLE_DAYS`)와 동일한 행 인덱스(0~9)에
  순환 배분 — 같은 정산주기 그룹은 같은 시각을 가짐(예: D+7 그룹은 전부 09:15). 4페이지 공통.
- **필터 UI**: 기존 이름 검색 옆에 `정산기간` datetime-local 인풋 2개(시작~종료)를 추가.
  라벨 힌트 "적용매출일 기준"으로 어느 컬럼을 대상으로 검색하는지 명시.
- **필터 로직**: 검색 시 `input[type=datetime-local]`의 값(`YYYY-MM-DDTHH:MM`)에서 `T`를 공백으로 바꿔
  테이블 셀의 "적용매출일" 텍스트(`YYYY-MM-DD HH:MM`, `tr.children[3]`)와 문자열 비교(`>=`/`<=`)만으로 판정.
  두 값 모두 고정폭 zero-padded 포맷이라 문자열 비교가 곧 시간 순서 비교와 같아서 별도 Date 파싱 불필요.
  이름 검색·기간검색은 AND 조건. 초기화 버튼은 이름·시작·종료 3개 필드를 모두 비움.
- **중국어판**: `정산기간→结算期间`, `적용매출일 기준→按适用销售日筛选` 2개 추가(공백 없는 "정산기간"은
  기존 "정산 기간"(공백 있음, 미사용 레거시 엔트리)과 별개 키라 새로 등록 필요했음).
- 브라우저에서 실제 필터 동작 확인: `2026-06-30 00:00~23:59` 범위로 검색 시 D+7/09:15 그룹(지사_01/05/09)만
  남고 나머지 숨김, 초기화 시 전체 복원됨을 확인.

### v3.0 — 정산 계산 공식 전면 재정의 (클라이언트 스펙 2026-07-16)
클라이언트가 제공한 4가지 계산식과 "당일 정산금액 산정 기준"(D+N 정산주기, 취소 차감, 결제 시점 확정,
소급 재계산 금지)을 반영해 정산 계산 엔진(settlement-v2.js)을 다시 짰다. **v2.4가 "공식 확정"이라고
선언했던 `수수료=round(총판매금액×수수료율)/정산예정금액=총판매금액−수수료`를 대체**하는 근본적인 변경.

**새 계산식** (사용자 제공 그대로):
1. 영업조직의 수수료 = 결제금액 × 수수료율 / 100
2. 상점(대리점)의 정산액 = 결제금액 − 플랫폼수수료 − 유보금
3. 유보금 = 결제금액 × 유보금비율 / 100
4. 플랫폼수수료 = 결제금액 × 플랫폼수수료율 / 100
→ **CA와 그 산하 조직(지사·총판·영업) 페이지의 "정산 금액"은 이제 수수료 그 자체(이득)**이고,
대리점 페이지만 "결제금액 − 수수료 − 유보금"(실제 지급액) 공식을 쓴다. 즉 지사/총판/영업 3페이지는
더 이상 "실매출액 − 수수료"가 아니라 **수수료 단독 값**을 정산 금액으로 표시.

**당일 정산금액 산정** (`todaySettlement(owner, ownerType, baseDate)`, settlement-v2.js):
1. 정산 대상 매출 = 기준일(baseDate, 기본값 TODAY) − 정산주기(D+N) = 적용매출일에 결제된 거래 합
2. 취소금액 차감 = 오늘(baseDate) 취소된 "이전 결제 건"(paidDate < baseDate)의 금액 합만 차감
   — 당일 결제 + 당일 취소는 완전히 제외(매출·차감 어느 쪽에도 안 잡힘)
3. 당일 정산 계산 기준금액 = 정산 대상 매출 − 취소차감액 (음수 가능, 클리핑하지 않고 그대로 표시)
4. 수수료/정산액은 기준금액에 수수료율(대리점은 +유보율)을 곱해 계산 — 이 결과가 목록 4페이지의
   "실매출액"(= 정산 대상 매출, 취소차감 전 값) · "정산 건수"(= 대상일 거래 건수) · "정산 금액" 3개 컬럼.
   실매출액 셀 아래 회색 서브텍스트로 "취소차감 -N원"을 표시(있을 때만).
5. 조직·수수료율 변경 소급 금지 규칙은 "결제 시점에 수수료가 확정·저장된다"는 개념으로 반영 —
   mock에서는 실제 요율 변경 시나리오를 만들지 않고 `owner.fee`를 항상 결제 시점 확정값으로 취급.

**mock 데이터 재설계** (`makeDetailRows`, settlement-v2.js):
- 거래마다 `paidDate`(기준일로부터 최대 `WINDOW_DAYS=16`일 이전까지 분산) · `cancelled`/`cancelDate` 필드 추가.
- 정산 대상일에 밀도를 높임(`i % 8 === 0`이면 강제로 그 날짜 배정)해 하루치 매출이 보통 여러 건으로
  구성되게 함 — 안 그러면 소액 취소 1~2건이 하루치 매출을 항상 압도해 기준금액이 상시 음수가 됨(실제로
  처음엔 그렇게 나와서 재조정함).
- 취소는 전체 이력에 확률적으로 걸지 않고 **대상일이 아닌 날짜의 거래 중 금액이 작은 순으로 1~2건만**
  "오늘 취소" 처리(`owner.count>=20`이면 2건). 마지막 거래 1건은 항상 "당일 결제+당일 취소" 데모로
  강제 배정(모든 계산에서 제외되는 케이스를 항상 시연 가능하게 함).
- 대리점(상점)만 결제일 순서로 순차 유보금 캡 적립(`retentionStatus(id).balance`를 시작 잔액으로 사용,
  기존 ca-022 유보금 원장과 같은 한도 로직 재사용) → 각 거래의 `retention`·`settle` 확정.
  **주의**: ca-022의 유보금 잔액(일별 dailyBases 누적)과 이 v3.0의 거래별 유보 차감은 서로 다른 계산
  경로로 존재하는 두 개의 독립된 추정치다(HANDOFF 6번 미해결 항목과 동일한 미조정 상태) — 실제 잔액을
  두 배로 차감하지 않도록 향후 백엔드 설계 시 반드시 하나로 통합해야 함.
- 영업조직(지사/총판/영업) 거래의 `settle` = 취소 건 제외하고 `fee` 그대로(이득 = 수수료).
- `txAgg`(목록 KPI)·`kpiAgg`(상세 KPI)는 취소 건을 전부 제외하고 집계하도록 수정.

**KPI 축 분리 유지** (사용자 확인): 상단 KPI 6종 카드(총 정산 수/완료/필요 건·금액)는 **여전히 거래
상태(정산필요→중→완료) 전체 이력 기준**이며, 이번 "오늘 정산 대상" 축과 무관하게 그대로 둠. 단, `settle`
공식 자체가 바뀌었으므로 KPI **금액**은 재계산되어 달라짐(지사/총판/영업은 수수료 단독이라 훨씬 작아짐).

**상세 페이지(ca-014) 개편**: "오늘 정산 대상만 보기" 체크박스(기본 켜짐)를 새로 추가.
- 켜짐: 적용매출일에 해당하는 거래 + 오늘 취소 차감 거래(회색 처리, 취소 배지, 체크박스 없음)만 노출.
- 꺼짐: 기존처럼 전체 이력 노출.
- KPI 6종은 이 토글과 무관하게 항상 전체 이력 기준(`kpiAgg()`, 목록 페이지와 일치).
- owner 카드에 "정산주기·정산 대상일·정산 대상 매출·오늘 취소 차감·당일 정산 계산 기준금액·
  수수료(이득)·(대리점만) 유보금 차감·오늘 지급 정산액/오늘 정산 이득" 블록 추가.
- 정산 기준일(`runBase`) 입력을 바꾸면 대상일도 같이 바뀜 — 단 mock의 취소 건은 `cancelDate`가 항상
  TODAY(2026-07-07)로 고정 생성되어 있어서, 07-07이 아닌 다른 기준일을 고르면 취소차감 라인은 보통
  안 나타난다(정산 대상일 자체는 정상적으로 이동함).
- 컬럼명 "지불해야 할 정산 금액" → "정산 금액", "판매 일시" → "결제 일시"로 통일(목록 페이지와 맞춤).

**목록 4페이지 재생성**: `todaySettlement`/`txAgg` 결과를 Node로 그대로 뽑아 KPI·행 데이터를 전부 교체.
브라우저에서 지사_01 상세 페이지 확인 결과, 목록의 실매출액(12,730원/취소차감 -3,150원)·정산건수(3건)·
정산금액(671원)이 상세 페이지 owner 카드 값과 정확히 일치함을 확인.

### v3.1 — 영업조직 수수료 계산에서 취소차감 제외 (사용자 정정 2026-07-16)
v3.0에서는 영업조직(지사·총판·영업)의 "정산 금액"도 대리점과 똑같이 "(실매출액 − 취소차감) × 수수료율"로
계산했는데, 사용자가 "수수료 = 지불해야 할 정산 금액 = 정산 금액 = 판매금액(매출액) × 수수료"라고
정정함 — **취소차감을 언급하지 않고 매출액×수수료율만으로 정의**. 이는 곧 영업조직의 수수료는 취소차감과
무관하다는 뜻으로 해석해 반영함:
- **영업조직(지사·총판·영업)**: 정산 금액 = 실매출액(정산 대상 매출, 취소차감 미반영) × 수수료율.
  다른 날짜에 발생한 취소는 이 계산에 전혀 영향을 주지 않음(결제 시점에 수수료가 이미 확정·저장된다는
  개념과도 일관됨).
- **대리점(상점)**: 기존 v3.0 그대로 유지 — 정산 금액 = (실매출액 − 취소차감) − 플랫폼수수료 − 유보금.
  취소차감은 상점의 실제 현금 정산에만 영향을 준다.
- `todaySettlement()`(settlement-v2.js)에서 영업조직 분기는 `fee(targetRevenue, owner.fee)`로,
  대리점 분기는 기존대로 `baseAmt`(= targetRevenue − cancelAmt) 기준으로 분리 계산.
- 목록 3페이지(ca-013/019/020, 대리점 ca-021은 변경 없음)에서 "취소차감 -N원" 서브텍스트를 제거하고
  정산 금액 값을 재계산해 반영(예: 지사_01 671원 → 891원 = 12,730원 × 7%).
- 상세 페이지(ca-014) owner 카드도 영업조직은 정산 대상 매출·수수료(=정산 금액)만 보여주도록 단순화하고,
  "취소된 다른 결제 건과는 무관 — 상점 정산에서만 차감"이라는 안내문을 추가. 취소 건은 대리점 페이지에서만
  "오늘 정산 대상만 보기" 토글에 노출되도록 `applyFilter()`도 분기.
- KPI 6종 카드(전체 이력 기준)는 영향 없음 — `makeDetailRows`의 영업조직 per-transaction `settle`은
  이미 v3.0부터 `r.fee`(취소차감 없는 값)였으므로 그대로 유지됨. 이번 정정은 `todaySettlement()`(오늘
  집계)에만 해당.

### v3.2 — 목록 페이지에서 정산 상태 관련 KPI 제거, 순수 대시보드로 전환 (사용자 요청 2026-07-16)
목록 4페이지(ca-013/019/020/021) 상단의 KPI 6종 카드(총 정산 수/정산 완료 건/정산 필요 건/정산 총액/
정산 완료 금액/정산 필요 금액)를 전부 제거. 이 카드들은 거래 상태(정산필요→중→완료) 전체 이력을
집계한 것이라 "정산 상태" 정보나 마찬가지였는데, 사용자가 "정산상태는 상세내역 보기에서 확인·처리하고
목록 페이지는 대시보드처럼 확인만 가능하게" 요청함에 따라 목록에서 상태 관련 수치를 전부 뺐다.
- **대체된 4카드**(상태 무관, 순수 오늘자 요약 — 아래 테이블과 완전히 같은 숫자의 합계라 서로 다른 걸
  말하는 KPI/테이블 불일치 문제도 해소됨): 정산 대상 (지사/총판/영업/대리점) 수(=10곳, 고정) ·
  오늘 정산 대상 매출(Σ 실매출액) · 오늘 정산 건수(Σ 정산 건수) · 오늘 정산 금액(Σ 정산 금액).
- 섹션 제목을 "기준일 정산 정보" → **"오늘의 정산 대시보드"**로, 안내 문구를 "정산 상태 확인·변경·
  지급완료 처리는 상세 내역 보기에서 진행합니다"로 바꿔 상태 처리 위치를 명시.
- **상세 페이지(ca-014)는 변경 없음** — 여전히 자체 KPI 6종 카드(전체 이력 기준, `kpiAgg()`)와 정산
  상태 필터·상태 변경·지급완료 버튼을 그대로 가지고 있고, 정산 상태 확인·처리는 계속 여기서만 한다.
- 4카드 합계 값은 v3.1 기준 각 owner의 `todaySettlement()` 결과를 Node로 합산해 하드코딩(예: 지사
  10곳/289,640원/32건/12,308원). settlement-v2.js 자체는 이번에 수정하지 않음(목록 페이지가 이미
  정적 스냅샷이라 표시 문구·숫자만 HTML에서 교체).
- 브라우저 확인: 지사·대리점 페이지에서 4카드 대시보드가 상태 언급 없이 표시되고, 아래 테이블·상세
  페이지와 여전히 연동(상세내역 보기 링크, 필터)됨을 확인.

### v3.3 — 대시보드 KPI 4카드 한 줄 정렬 (사용자 요청 2026-07-16)
v3.2에서 6카드→4카드로 줄인 뒤에도 `.settlement-kpi-grid`가 `grid-template-columns:repeat(3,1fr)`라
3+1로 줄바꿈되던 것을 한 줄로 정렬. shared.css에 `.settlement-kpi-grid--4 { grid-template-columns:
repeat(4,1fr); }` 모디파이어 클래스를 추가하고, 목록 4페이지(ca-013/019/020/021)의 KPI 그리드 div에
`settlement-kpi-grid--4` 클래스를 덧붙임. 반응형 브레이크포인트(900px 이하 2열, 560px 이하 1열)는
기존 `.settlement-kpi-grid` 규칙이 그대로 적용되므로 손대지 않음. 상세 페이지(ca-014, 6카드)와
ca-022(4카드지만 이번에 건드리지 않음 — 필요하면 같은 클래스를 추가하면 됨)는 영향받지 않음.

### v3.4 — 정산기간 검색에서 시간 제거 (사용자 요청 2026-07-16) ★현재
v2.10에서 도입한 "정산기간" 검색(시작~종료)이 `datetime-local`(날짜+시간)이었는데, 시간 없이 날짜만으로
검색하도록 단순화. 목록 4페이지(ca-013/019/020/021) 공통 변경:
- 필터 인풋을 `input[type=datetime-local]` → `input[type=date]`로 교체.
- 필터 스크립트에서 셀 값 비교 시 `tr.children[3].textContent.trim()`(예: `2026-06-30 09:15`)를
  `.slice(0,10)`으로 날짜 부분만 잘라 비교(`applied >= from && applied <= to`). 인풋 값도 `date` 타입이라
  이미 `YYYY-MM-DD`라 별도 변환 불필요(기존엔 `T`를 공백으로 바꿔주는 코드가 있었는데 제거).
- **테이블에 표시되는 "적용매출일" 셀 값(날짜+시간)은 그대로 유지** — 시간을 뺀 건 검색 입력·비교
  방식만이며, 화면에 보이는 정보량은 줄이지 않음.
- 주의: 이번 변경은 브라우저 스크린샷 도구가 반복적으로 타임아웃되어 시각 확인을 못 했음(문법 검사만
  통과). 다음 세션에서 실제 필터 동작을 한 번 확인해볼 것.

## 4. 데이터 모델 (v2.4 기준 — settlement-v2.js 내 mock)

```
BRANCHES / DISTS / SALESREPS / BRANDS   // 페이지별 집계 로우 (고정 데모 값)
  { id, name, fee(%), sales(총판매금액), count(정산 건수),
    parentName·grandName(조직 소속) | source·managerName(대리점) }
RETENTION_CONF { brandId(bd01~bd10): { rate(%), limit(원) } }
RET_LOGS / DEP_LOGS { brandId: [{at, type, amount, reason, by}] }  // 수동 이벤트 원장
statusStore { "type|id|run|기준일": { status, history[] } }        // 기준일별 상태
```

핵심 함수:
- `fee(sales, rate)` = round(sales × rate / 100) — 수수료
- `toRow(src, type, ck)` — 목록 로우: payable = sales − fee ← **확정 공식**
- `makeDetailRows(owner, type)` — 상세 분해: count건 거래, 판매금액 합 = sales,
  수수료 합 = fee (마지막 행이 반올림 흡수) → 목록·상세 항상 일치
- `dailyBases(brand)` — 총판매금액을 7일로 분해 (유보 적립 산출용)
- `retentionLedger(brandId)` — 일별 자동 적립(일매출×유보율, 한도 캡) + 수동 이벤트 재생
- `retentionStatus(brandId)` — {balance, overLimit(한도 도달), needUseReg(90일+), heldDays…}

상태 전이표: 필요→[대기,보류] / 대기→[중,보류] / 중→[완료,보류] / 완료→[중(=완료 취소, 사유 필수)] / 보류→[필요].
지급완료는 '정산중' 상태만, 기준일 단위로만 기록.

데모 케이스: 대리점_09 = 유보 한도(30,000원) 도달 → ⚠인출 필요 / 대리점_03 = 3/15 이월 적립 → ⏱90일+ 사용등록 필요.

## 5. 검증 방법 (수정 후 반드시 실행)

브라우저 없이 Node로 로직 검증하는 패턴 (mock DOM 스텁 + 디버그 훅 주입):

```bash
node --check settlement-v2/settlement-v2.js   # 문법
# 로직: "/* ───────── 11. 부트스트랩" 마커 앞에 window.__v2={...내부 함수 노출...} 를
# sed로 주입한 임시 사본을 만들고, window/document/location 스텁과 함께 require 후 수식 검증.
# (이전 세션에서 사용한 검증 항목: 지급 공식, 원금 기준 유보 적립·한도 캡, 당일취소 제외,
#  분배 합계=원금 100%, CA 마진, 조직 취소 차감, 주기 매칭, 상세-목록 집계 일치, 90일/한도 알림)
```

shared.js 의존 API: `A.formatCurrency, badge, settlementBadge, renderPagination, showToast, setBodyLocked, downloadCSV, formatDate`.

## 6. 미해결 / 정책 확인 필요 (다음 작업 후보)

1. ~~조직 페이지 '정산 예정 금액'의 의미~~ → **v2.4에서 해소** (레퍼런스 테이블로 매출 − 수수료 확정).
2. **v2.2 취소·주기 정책의 화면 반영** — "당일 취소 확정분 차감, 주기 도래 매출 기준, 당일주문
   당일취소 제외" 정책은 여전히 유효하나 v2.4 레퍼런스 레이아웃에는 해당 컬럼이 없음.
   백엔드 연동 시 총판매금액 산출 로직에 반영해야 함 (표시 여부는 클라이언트 확인).
3. **유보금 차감의 표시 위치** — 지급 시 유보금 추가 차감(v2.2 피드백)은 유효하나 목록 컬럼에는
   미표시(레퍼런스 준수). 실지급액 = 정산 예정 금액 − 유보 적립. 표시 여부 확인 필요.
4. **유보금 적립 시점** — 현재 '일매출 기준 일별 적립' mock. "결제 시마다" 즉시 적립 여부는
   백엔드 설계 시 결정.
5. **완료 취소 권한 분리** — 현재 사유 입력만으로 허용. 실서비스에서는 권한 필요할 수 있음.
6. mock 상태(statusStore, RET_LOGS 등)는 **새로고침 시 초기화** — 프로토타입 한계.
7. 원본 어드민의 다른 화면(조직/대리점/주문 등)은 미개선 — 정산 모듈만 v2화됨.

## 7. 작업 규칙 (이 프로젝트에서 지켜온 것)

- 원본 폴더(SJhub-CA 루트) 파일은 수정하지 않음. 개선은 settlement-v2/에만.
- 모든 화면 수치는 SALES 단일 원천에서 파생 — 하드코딩 KPI 금지.
- 원장(유보금/예치금)은 삭제 금지, 역분개로 정정.
- 수정 후: `node --check` + 로직 스텁 테스트 → index.html의 버전 표 갱신 → 사용자에게 정책 확인
  필요 항목을 명시적으로 보고.
- 사용자(Leona)는 간결·직접적인 한국어 보고 선호. 클라이언트 피드백은 중국어로 올 수 있음(번역 필요).
```
